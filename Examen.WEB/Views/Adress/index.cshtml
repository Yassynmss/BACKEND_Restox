@model IEnumerable<Examen.ApplicationCore.Domain.Adress>

@{
    ViewData["Title"] = "index";
}

<h1>index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Line1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Line2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ville)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Line1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Line2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ville)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdressID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdressID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdressID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
